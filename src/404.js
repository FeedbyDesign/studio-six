import React from 'react'

const errorStyle = {
  textAlign: "center",
  margin: "auto",
  paddingBottom: "20vh"
}

const Error = () => (
  <h1 style={errorStyle}>Sorry, this page does not exist</h1>
)

export default Error
